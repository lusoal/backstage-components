apiVersion: v1
kind: Service
metadata:
  name: agent-runtime
  namespace: agent-platform
  labels:
    app: agent-runtime
spec:
  type: ClusterIP
  selector:
    app: agent-runtime
  ports:
    - name: http
      port: 80
      targetPort: 8080
